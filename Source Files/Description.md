The files in this directory are all source code files.
There are also other files like our desktop config file, shell script which runs on startup.
There is also a file which keeps track of an argument.
